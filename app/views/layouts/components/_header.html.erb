<nav class="navbar is-dark is-fixed-top" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to "Standup App", root_path, class: "navbar-item has-text-weight-bold" %>
  </div>

  <div class="navbar-menu">
    <div class="navbar-end">
      <div class="navbar-item has-dropdown" data-controller="dropdown">
        <a class="navbar-link" data-action="dropdown#toggle click@window->dropdown#hide">
          <% if current_user %>
            Hi, <%= current_user.name %> (<%= current_account.name %>)
          <% else %>
            Hi, Guest
          <% end %>
        </a>
        <div class="navbar-dropdown is-right" data-dropdown-target="menu">
          <a class="navbar-item" href="#" data-action="dropdown#toggle">Profile</a>
          <a class="navbar-item" href="#" data-action="dropdown#toggle">Settings</a>
          <hr class="navbar-divider">
          <a class="navbar-item" href="#" data-action="dropdown#toggle">Log Out</a>
        </div>
      </div>
    </div>
  </div>
</nav>
